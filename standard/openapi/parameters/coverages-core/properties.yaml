name: properties
in: query
description: |-
  Select specific data record fields (measured/observed properties) to be returned.
  The selected field names must be one of the fields defined in the collection's logical schema (the fields of the RangeType DataRecord in the Coverage Implementation Schema model).

required: false
schema:
  type: string
