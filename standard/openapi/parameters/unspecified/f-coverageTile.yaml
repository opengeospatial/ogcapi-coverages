name: f
in: query
description: The format of the tiled coverage data response (e.g. tiff). Accepted values are 'tiff' (GeoTIFF), 'netcdf', or 'png'.
required: false
schema:
  type: string
  enum:
    - png
    - geotiff
    - netcdf
    - covjson
style: form
explode: false
