type: object
required:
  - links
properties:
  title:
    type: string
    example: Coverages over Bonn
  description:
    type: string
    example: Access to coverages which include the city of Bonn via a Web API that conforms to the OGC API Coverages specification.
  links:
    type: array
    items:
      $ref: common/link.yaml
