[[rc-scaling]]
== Requirements Class "Scaling"

=== Overview

The "Scaling" Requirements Class defines the `scale-factor`, `scale-axes`, `scale-size`, `width` and `height` parameters for retrieving coverage data at a specific resolution.

=== Scaling Examples

See also <<scaling-examples, examples of scaling requests>> in an annex.

include::requirements/requirements_class_coverage_scaling.adoc[]
