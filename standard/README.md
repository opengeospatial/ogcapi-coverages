# OGC API (OAPI) Coverages Standard

This directory contains the OGC API - Coverages Standard. This OGC Standard, working with the parallel OGC API Common specification, defines the API analog to the WCS standard.

This OAPI Coverage Standard addresses only those parts of an API which are specific to Coverage resources. Those capabilities which may have applicability beyond coverages should be considered for inclusion in Common.
